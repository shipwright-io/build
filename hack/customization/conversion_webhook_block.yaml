spec:
  conversion:
    strategy: Webhook
    webhook:
      clientConfig:
        service:
          namespace: shipwright-build
          name: shp-build-webhook
          path: /convert
      conversionReviewVersions:
      - v1